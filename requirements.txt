# Python 3.14.4
# Current scope: dataset download, collection, raw-data inspection, and preprocessing.
# Model training/UI/DB dependencies are intentionally not included yet.
# Add streamlit, scikit-learn, xgboost, lightgbm, shap, sqlalchemy,
# psycopg2-binary, and joblib when those implementation files exist.

# Kaggle datasets and CLI fallback
kagglehub>=1.0.0
kaggle>=1.7.4

# HTTP API collectors: HenrikDev, Riot API, valorant-api.com
httpx>=0.28.0
requests>=2.32.0
python-dotenv>=1.1.0

# Riot VCT S3 anonymous download
boto3>=1.38.0
botocore>=1.38.0
tqdm>=4.67.0

# Scraping/parsing fallback sources: VLR.gg and Liquipedia
beautifulsoup4>=4.13.0
lxml>=5.4.0
liquipediapy

# CSV loading and dataset sanity checks
numpy>=2.2.0
pandas>=2.3.0
